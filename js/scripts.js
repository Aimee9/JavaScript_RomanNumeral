var getNumerals = function(numbers) {

  var romanNumeral = new Array();

    for(var i = 0; i < numbers; i++) {
       if (number <= 3999;)
       ;
    }



//     var numerals = romanNumeral.join();
//    numerals = numerals.replace(/[,]/g,"");
//
//   return numerals;

 };


$(document).ready(function() {
  $("form#RomanNumerals").submit(function(event) {
    var numbers = parseInt($("input#numbers").val());
    //numbers = getnumerals(numbers);
    $(".numeral").text(numerals);


     $("#result").show();
    event.preventDefault();
   });
});
